export default function HomePage() {
  return (
    <main style={{ fontFamily: "ui-sans-serif, system-ui", padding: 32 }}>
      <h1>AgentGate</h1>
      <p>
        Governance API + reviewer dashboard for autonomous agents. The frontend
        team owns this page — for now it is a placeholder so the backend can
        boot.
      </p>
      <p>
        API base path: <code>/api/v1</code>
      </p>
    </main>
  );
}
